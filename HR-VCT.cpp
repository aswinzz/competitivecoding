#include <sstream>
#include <vector>
#include <iostream>
using namespace std;



int main() {
    string str;
    int a,b,c;
    cin >> str;
    vector<int> integers;
    ss<<a<<ch<<b<<ch<<c;
    integers.push_back(a);
    integers.push_back(b);
    integers.push_back(c);
    for(int i = 0; i < integers.size(); i++) {
        cout << integers[i] << "\n";
    }


    return 0;
}
